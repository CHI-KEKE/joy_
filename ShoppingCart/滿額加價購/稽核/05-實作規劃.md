# 05 實作規劃

## 新增 Processor

**類別名稱**：`CartExtraPurchaseAuditProcessor`

**位置**：`src/Nine1.Commerce.Console.BL/Services/AuditProcessor/`

---

## AuditProcessorDefinitionCenter 登記

```csharp
public static readonly List<Type> OrderPlaceAuditProcessorLayers = new()
{
    typeof(OrderInfoProcessor),
    typeof(OrderCouponDataAuditProcessor),
    typeof(CouponRedeemAuditProcessor),
    typeof(SalepageExtendInfoAuditProcessor),
    typeof(SalepageBundleAuditProcessor),
    typeof(CartExtraPurchaseAuditProcessor),  // 新增
};
```

DI 透過 `foreach` 自動掃描 `OrderPlaceAuditProcessorLayers` 注冊，不需另外處理。

---

## 待確認事項

| # | 問題 | 影響 |
|---|---|---|
| 1 | 限購上限 `LimitQty` 的資料從哪裡取（PayProcess Cache 找不到 `AddOnsQtyLimit`）| 限購稽核邏輯 |
| 2 | `IsForCache = false` 是否需實作（從 DB 補查）| 實作範圍 |

---

## 參考

- Cache Key 格式：`PayProcess-20190918180000`
- 活動 TypeDef：`CartReachPriceExtraPurchase`
- 商品加價購 TypeDef（對照）：`AddOnsSalepageExtraPurchase`
