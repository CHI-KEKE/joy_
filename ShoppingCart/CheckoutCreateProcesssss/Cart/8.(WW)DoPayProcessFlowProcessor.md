轉打PayProcessUrl的流程


## ConvertMemberCartToWebStoreGetCart

#### MappingOldCartSelectedEntity



#### 如果是重量計價 ShippingAreaId需要填到洲的維度

```csharp
bool isSelectedDomesticWeightPricing = shoppingCartV2.SelectedCheckoutShippingTypeGroup?.DeliveryTypeList?.Where(x => x.IsSelected && x.IsDomesticWeightPricing()).Any() == true;
if (isSelectedDomesticWeightPricing && cartEntity.SelectedShippingArea != null)
{
    shoppingCartV2.SelectedShippingArea = cartEntity.SelectedShippingArea.Clone();
    shoppingCartV2.SelectedShippingArea.Id = shoppingCartV2.SelectedShippingArea.SubShippingAreaId;
}
```



## 發送 entity


ShoppingCartClientEntity

CheckoutType (ShoppingCartClientCheckoutEntity)


SelectedCheckoutShippingTypeGroup (ShoppingCartClientShippingTypeGroupEntity)

SelectedShippingArea (ShippingAreaEntity)

SubShippingAreas (IEnumerable<ShippingAreaEntity> )

