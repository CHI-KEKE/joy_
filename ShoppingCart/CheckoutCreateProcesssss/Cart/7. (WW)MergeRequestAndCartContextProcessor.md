

## checkoutEntity.SelectedShippingArea.Id

//// 配送地區
var shippingAreaId = checkoutEntity.SelectedShippingArea.Id;





## SubShippingAreaId


## 宅配成立

```csharp
checkoutShippingType.ShippingProfileTypeDef == nameof(ShippingProfileTypeDefEnum.Home) || checkoutShippingType.ShippingProfileTypeDef == nameof(ShippingProfileTypeDefEnum.CashOnDelivery
&& checkoutShippingType.AddressBookId == 0)
```


## 重量計價成立


```csharp
var selectedDeliveryType = checkoutShippingType.DeliveryTypeList.FirstOrDefault(x => x.IsSelected);
bool isDomesticWeightPricing = checkoutShippingType.ShippingProfileTypeDef == nameof(ShippingProfileTypeDefEnum.Home)
    && selectedDeliveryType?.IsDomesticWeightPricing() == true;
```

## 有選到某個 subShippingAreaId

```csharp
var weightProfile = await this._shopShippingWeightProfileService.GetShopShippingWeightProfileAsync(context.ShopId, selectedDeliveryType.Id);
var selectedSubShippingAreaId = context.CheckoutEntity?.SelectedShippingArea?.SubShippingAreaId;
if (selectedSubShippingAreaId > 0)
{
    //// 優先以計算用的地區選取
    var weightProfileShippingArea = weightProfile.ShippingAreaList.FirstOrDefault(x => x.ShippingAreaId == selectedSubShippingAreaId);
    receiver = checkoutEntity.DisplayReceiver.FirstOrDefault(r => r.ShippingProfileTypeDef == checkoutShippingType.ShippingProfileTypeDef
                                                                && weightProfileShippingArea?.ShippingAreaName == r.State);
}
```

==> 我預期會變成這樣 checkoutShippingType.AddressBookId = receiver?.AddressBookId ?? 0;
