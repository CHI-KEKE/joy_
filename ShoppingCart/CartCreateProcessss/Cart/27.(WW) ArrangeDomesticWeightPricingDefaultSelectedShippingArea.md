

## 不往下走的情況


1. hasDomesticWeightPricingDeliveryType false

bool hasDomesticWeightPricingDeliveryType = context.Data.CheckoutType?.DeliveryTypeList?.Any(item => item.IsDomesticWeightPricing()) == true

false 不會往下走

2. 沒有 SelectedShippingArea
if (context.Data.SelectedShippingArea == null) 



## 開始

####　CheckoutCreat時 要把常用收件人納入判斷


#### GetDomesticWeightPricingShippingAreaIdAsync



context.Data.SelectedShippingArea.SubShippingAreaId = selectedShippingAreaId;

